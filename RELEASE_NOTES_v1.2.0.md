# 七款小游戏 Windows 便携版 v1.2.0

这是七款小游戏合集的**质量与内容**版本：在 v1.1.0 全部内容的基础上，做了一次全仓库代码审计，
修复了 7 款游戏中影响玩法正确性的逻辑漏洞，并补齐了此前"做了但玩不到"的内容。

源码仓库：[qjngbac/seven-games](https://github.com/qjngbac/seven-games)

## 包含的游戏

1. 荒诞情境判断游戏
2. 伪装者逻辑推理游戏
3. 错误操作模拟器
4. 有限信息审查游戏
5. 多人对话推理游戏
6. 规则变化反应判断游戏
7. 物品组合解谜游戏

## 本版新增内容

- **错误操作模拟器：新增第七章「工具箱补齐」，11 张全新工单。** 覆盖此前从未出现过的故障场景：
  无线 AP 掉线、台式机断电 / 显示器损坏 / 系统盘写满、服务器磁盘满导致关键服务停摆、
  感应门控制器死机与门体卡阻、摄像头夜视失效与适配器损坏、单路网线接头松脱、市电与 UPS 双保险全断。
  工单总数从 32 张扩展到 **43 张**，此前 8 个"定义了却永远点不动"的修复动作现已全部可用。
- **错误操作模拟器：新增因果链**——服务器磁盘写满会拖垮关键服务（原先磁盘满不产生任何可观察症状）。
- **荒诞情境判断游戏：新增 5 个「资源危机」专属事件。** 只在对应处境下才会出现：
  技术债 ≥ 70、精神 ≤ 25、金钱 ≤ 20、声誉 ≤ 25，以及复合条件「技术债 ≥ 50 且精神 ≤ 35」的凌晨告警。
- **荒诞情境判断游戏：日结页新增「资源走势折线图」**，可以直观看到这一周金钱 / 声誉 / 精神 / 技术债各自的走向。
- **荒诞情境判断游戏：部分选项加入前置条件**，例如「找老板谈涨薪」需要先与老板建立过沟通才会出现。

## 本版修复

- **错误操作模拟器**：修复「先恢复出厂毁掉摄像头配置、再修好原故障，仍被判为满分根因修复」的问题。
  现在不可逆损失会如实披露，评价最多只能拿到 C；破拆等同类不可逆操作也一并纳入限制范围。
- **错误操作模拟器**：结算后不能再继续操作，重复提交不再重复计分。
- **有限信息审查游戏**：修复「材料缺失被当成字段填错」的问题，拒绝理由现在准确无误。
  借此修正了两处规则——务工人员的工作许可、携带动物的检疫证，应当是**必须出示证件**而非"填对字段"。
- **有限信息审查游戏**：全部选择「暂扣」的一天不再把累计准确率拉低。
- **多人对话推理游戏**：修复「在结算页刷新后可重复提交指控、反复刷声誉」的问题。
- **多人对话推理游戏**：共同责任案件（雪崩之夜）允许先指认部分责任人，并会提示补齐。
- **规则变化反应判断游戏**：修复「极限混战 / 逻辑组合规则包中，有规则在 30 轮内永远不会被揭示」的问题，
  现在每条规则都会依次登场；每日挑战种子改用 UTC，保证同一天题目一致。
- **物品组合解谜游戏**：修复「先用网线捅进风口会消耗网线，导致专业解法再也做不到」的问题。
- **物品组合解谜游戏**：修复「多标签物品会命中两条同级配方而取错解法」的问题，
  改为由更具体的规则优先（例如橡胶块现在稳定对应专业解法）。
- **荒诞情境判断游戏**：修复首日曲线未计入开局特质、存档损坏时「继续游戏」没有任何提示的问题。
- **通用**：开发与预览服务默认只监听 `127.0.0.1`，需要局域网访问时再显式开启。

本轮修复后，七款游戏的单元测试从 294 项增加到 **348 项**，全部通过。

## 玩家环境要求

- Windows 10 或 Windows 11
- 现代网页浏览器
- 不需要安装 Node.js 或 Python
- 下载完成后不需要联网

## 运行方法

1. 在下方附件区域下载 **`Games.zip`**。
2. 完整解压 ZIP，不要直接从压缩包预览中运行。
3. 打开 `Seven-Games-Windows-Portable` 文件夹。
4. 双击 `开始游戏.cmd`。
5. 游戏期间保持启动窗口开启。

启动器只通过 `http://127.0.0.1:5200/` 提供本地游戏服务。游戏进度和设置保存在
当前浏览器的本地存储中。

GitHub 自动生成的 **`Source code (zip)`** 不是可直接运行的游戏包。玩家应下载
Release 附件中的 `Games.zip`。

## 完整性校验

本版本同时提供 `SHA256SUMS.txt`。

```text
3BD72FB264455598B118AF5BF908590C0FDF5CB5C8889E93DA0283F4EE7E6B99 *Games.zip
```

PowerShell 校验命令：

```powershell
$expected = ((Get-Content .\SHA256SUMS.txt -Raw).Trim() -split '\s+')[0]
$actual = (Get-FileHash -Algorithm SHA256 .\Games.zip).Hash
if ($actual -ne $expected) { throw 'Games.zip 校验值不匹配' }
"SHA-256 校验通过：$actual"
```

## 许可证

源码采用 GNU 通用公共许可证第 3 版（`GPL-3.0-only`）发布。
